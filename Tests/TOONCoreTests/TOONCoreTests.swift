import XCTest
@testable import TOONCore

final class TOONCoreTests: XCTestCase {
    func testCoreNotImplementedPlaceholder() {
        XCTAssertTrue(TOONCoreStatus.message.contains("pending"))
    }
}
